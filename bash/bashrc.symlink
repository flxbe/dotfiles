#!/bin/bash

# export DOT environment variable
export DOT=$HOME/./dotfiles

# add the /DOT/bin folder to the path
export PATH="$DOT/bin:$PATH"

# import .bash_local
if [ -f ~/.bash_local ]; then
    source ~/.bash_local
fi

